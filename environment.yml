name: quick-sell
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # API & server
      - fastapi==0.115.0
      - uvicorn[standard]==0.30.6
      - python-multipart==0.0.9
      - httpx==0.27.0

      # AI providers
      - anthropic==0.34.0
      - openai==1.45.0
      - google-genai==0.8.0

      # Walidacja danych
      - pydantic==2.8.2
      - pydantic-settings==2.4.0

      # Frontend
      - streamlit==1.38.0
      - pillow==10.4.0

      # Testy
      - pytest==8.3.3
      - pytest-asyncio==0.24.0
      - pytest-cov==5.0.0

      # Jakość kodu
      - ruff==0.6.5

      # Env / konfiguracja
      - python-dotenv==1.0.1